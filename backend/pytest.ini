[pytest]
# Ensure 'app' package is importable from anywhere in the repo.
# Run tests with:  pytest tests/ -v  (from backend/)
# Or:              pytest backend/tests/ -v  (from repo root)
pythonpath = .
testpaths = tests
